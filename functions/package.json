{
  "name": "qa-saas-functions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "echo \"Add lint\"",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "main": "lib/index.js",
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "stripe": "^16.9.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
